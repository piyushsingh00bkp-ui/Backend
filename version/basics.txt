Check Version via Terminal (Command Line) -> node-v or node --version

Semantic Versioning -> node js uses semantic Versioning

 1.0.0 ---- MAJOR.MINOR.PATCH

 PATCH version (1.0._) — Bug Fixes: This number changes when backward-compatible bug fixes, 
 security patches, or performance optimizations are released. No new features are added here.
 (optional)

 MINOR version (1._.0) — New Features: This number changes when new, backward-compatible
  functionality or features are added. Your existing code will still work perfectly fine
   without any changes.(Recommended bug fix)


   MAJOR version (_.0.0) — Breaking Changes: This number changes when major rewrites or breaking
    API changes are introduced. If you upgrade a package across a Major version,
    your existing code might break, and you will likely need to update your code.(Important and mandatory)




    Caret (^)Minor and Patch updates (No breaking changes)^1.2.31.2.3 up to < 2.0.0 (e.g., 1.5.0, 1.9.9)
    (Install all the recommended and minor fixes automatically)


    Tilde (~)Patch updates only (Bug fixes only)~1.2.31.2.3 up to < 1.3.0 (e.g., 1.2.4, 1.2.9)Exact VersionNo updates allowed1.2.31.2.3 only
    (Approximately eq to the version only changes the 1.2.1,1.2.2 like this not 1.2.1,1.3.1 not this)



    Wildcard (*)Any update (Dangerous)*Any version (0.5.0, 1.2.3, 5.0.0, etc.)
    ()
